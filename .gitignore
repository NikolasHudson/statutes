# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Django
*.log
db.sqlite3
db.sqlite3-journal
staticfiles/
media/

# Environment / secrets
.env
.env.local
.env.*.local

# Editors / IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Postgres corpus dumps — large binaries, not source. Keep out of git
# (use object storage / a release asset to share these).
*.dump

# Local ingestion data — regenerable bulk caches / raw artifacts (multi-GB)
backend/data/raw/

# Local-only: prod DB connection string for cloning (never commit)
.prod_db_url

# Prod DB snapshot from clone_prod_db.sh — never commit
prod.dump
